{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/index.js",
      "args": [],
      "env": {
        "INFLUXDB_URL": "http://localhost:8086"
      },
      "cwd": "${workspaceFolder}",
      "protocol": "inspector",
      "preLaunchTask": "Single Build"
    }
  ]
}
